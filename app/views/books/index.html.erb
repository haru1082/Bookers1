 
 <p id="notice"></p>
  <h1>Books</h1>

  <table>
   <thead>
    <tr>
　   <th class = "th-title">Title</th>
     <th class = "th-body">Body</th>
    </tr>
   </thead>
   <tbody>
   <% @books.each do |book| %>
    <tr>
     <td>
      <span class = book-title><%= book.title %></span>
     </td>
     <td>
      <span class = book-body><%= book.body %></span>
     </td>
     <td>
      <a class= "show" href= "<%= book_path(book) %>">Show</a>
     </td>
     <td>
      <a class= "edit" href= "<%= edit_book_path(book) %>">Edit</a>
     </td>
     <td>
      <a data-confirm ="Are you sure?" class = "destroy" rel = "nofollow" data-method ="delete" href="<%= book_path(book) %>">Destroy</a>
     </td>
    </tr>
   <% end %>
   </tbody>
  </table>


<h1>New Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


 <%= form_with model: @book do |f| %>
  <div class="field">
   <h4>Title</h4>
   <%= f.text_field :title %>

   <h4>Body</h4>
   <%= f.text_area :body %>

   <p><%= f.submit 'Create Book' %></p>
  </div>
 <% end %>
